{
  "mcpServers": {
    "ask_nlw": {
      "command": "/Users/guha/mahi/v1/bin/python",
      "args": [
        "-c", 
        "import sys; import os; sys.path.insert(0, os.getcwd()); from tools.chatbot_interface import serve; import asyncio; asyncio.run(serve())",
        "--server",
        "http://localhost:8000",
        "--endpoint",
        "/mcp"
      ],
      "cwd": "/Users/guha/NLWeb/code"
    }
  }
}
